{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "description": "NovelSaga LSP Client",
  "main": "./out/extension.js",
  "scripts": {
    "compile": "node build.mjs",
    "watch": "node build.mjs --watch",
    "lint": "eslint src --ext ts",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
